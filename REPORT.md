# AI-DPD 技术报告：用神经网络做数字预失真，从训练到定点可部署

**日期**：2026-09-13 · **仓库**：https://github.com/yluo008-lang/ai-dpd · **提交**：`bdc2e1f`

---

## 摘要

本报告记录一个**可训练、可量化、可部署的 AI-DPD（神经网络数字预失真）工程**的完整方法、实验与结论。
核心结果：

- **混合 DPD（记忆多项式支路 + 神经残差）** 在 int16 定点下达到 **−70.43 dB ACLR / 0.0515% EVM**，与浮点只差 **+0.04 dB**，追平纯多项式 DPD。
- 从 **PyTorch 训练 -> int16 量化 -> C 参考推理 -> Verilog 数据通路**，关键模块全部做**位真对拍**。
- 揭示并修复了数个**真实工程陷阱**（见第 7 节），其中 "峰值余量标定" 是定点 DPD 的关键设计规则。

---

## 1. 背景与动机

功放（PA）在接近饱和时呈非线性 + 记忆效应。对高峰均比的 OFDM/5G-NR 信号会产生带外扩展（恶化 ACLR）与带内失真（恶化 EVM）。DPD 在 PA 前放一个它的逆，使 `PA(DPD(x)) ≈ G·x`。

传统 DPD 用**记忆多项式 (GMP/MP)**，假设 PA 行为静态；宽带/多模/器件漂移下不够用。近期研究转向神经网络 DPD（NN-DPD）。本工程的目标：**把这条链路做完整、并诚实量化它的真实收益与代价**。

## 2. 功放模型（用于开发与标定）

- **MP（记忆多项式）**：`y(n)=Σ_m Σ_j a_{j,m} x(n−m)|x(n−m)|^{2j}`，M 抽头 × Np 奇数阶。
- **GmMP（广义 MP）**：加入交叉记忆项。
- **IQMP（MP + I/Q 失衡）**：`y = MP(x) + b·conj(MP(x))`。`conj` 项是**反解析**的，**记忆多项式原理上无法表示** -> 这是能"证伪/证实 NN 价值"的关键场景。
- 另提供 torch 可微版本，供直接学习使用。

## 3. DPD 架构

- **ILA（间接学习）**：训练后失真器 `D(y/G) ≈ x`，系数拷贝到前级。简单，但有**域偏移**（训练用 `y/G`、部署用 `x`）。
- **DLA（直接学习）**：`min ‖PA(D(x)) − G·x‖²`，PA 进环，消除域偏移。需要一个**可微 PA**。
- **Hybrid（本工程最优）**：`D(x) = W_mp·φ_MP(x) + NN(x)`，多项式支路（线性、精确逆）**用 LS 解初始化**，神经残差零初始化，端到端 DLA 训练。

## 4. 神经网络模型

| 模型 | 结构 | 特点 |
|---|---|---|
| **RVTDNN** | MLP on `[I_m,Q_m,|x_m|]` 时延特征 + 残差 | 最易量化、最常用 |
| **CVNN** | 复数线性层 + modReLU | 天然匹配 I/Q |
| **TCN** | 因果膨胀卷积 (dil 1,2,4,8) + 线性头 + 残差 | 长记忆、感受野大 |
| **Hybrid** | MP 基线性 + MLP 残差 | **精度王** |

## 5. 训练要点（都是踩出来的）

1. **整段连续序列训练**：时延 tap 用 `roll` 生成，若再随机 batch，tap 会取到别的随机样本 -> 记忆学不到。
2. **残差支路零初始化**：`D(x)=x+NN(x)` 起点即恒等 -> **单项就 +≈6 dB**。
3. **多项式支路 LS 初始化**：Hybrid 随机初始化只有 −46.9 dB，LS 初始化后 -> −70.9 dB。
4. **学习率别太保守**：lr≈1e-2 才跳出 ~−40 dB 局部最优。

## 6. 量化与部署

- **int16 权重 + 激活**，逐层 scale；整数 requant：`out = (acc·REQ + (BQ<<SHIFT) + (1<<(SHIFT-1))) >> SHIFT`。
- **Python / C / Verilog 共用同一条整数通路**（位真一致）。
- **峰值余量标定（关键规则）**：定点 scale 必须覆盖**工作范围**，而非只覆盖标定集；否则更大的峰值会**削顶**，而**这些峰值正是 PA 压缩最狠、DPD 最需要发力的地方**。
- **校正保真度**要测在**校正量 `z−x`** 上；测整段输出（≈x，功率占绝对主导）会把保真度显著**夸大**。

## 7. 实验结果

### 7.1 匹配 vs 失配 PA（5 seed 均值±std，drive=0.25）

| 方法 | MP PA（匹配）ACLR/EVM | MP+I/Q失衡（失配）ACLR/EVM |
|---|---|---|
| no-DPD | −46.35 / 3.80% | −45.71 / 6.33% |
| **PolyMP** | **−70.49** / 0.072% | −54.45 / **5.11%** |
| ILA-NN | −49.97 / 0.455% | −48.72 / **0.590%** |
| DLA-NN | −51.31 / 0.367% | −49.18 / **0.503%** |
| TCN | −45.78 / 0.827% | −46.30 / 0.794% |

**结论**：匹配 PA 上多项式是天花板（−70 dB）；**失配 PA 上多项式的 EVM 能力崩了**（6.33%->5.11%，几乎没修），而 **NN 把 EVM 压到 0.5%**——NN 的价值体现在**多项式无法建模的失真**上。

### 7.2 驱动扫描（ACLR dB，MP PA）

| drive | noDPD | Poly | ILA | DLA |
|---|---|---|---|---|
| 0.15 | −55.6 | −79.4 | −55.8 | −53.5 |
| 0.25 | −46.4 | −70.1 | −52.7 | −49.9 |
| 0.35 | −39.8 | −51.3 | −44.1 | −44.5 |

### 7.3 序列长度 N 的影响

| N | Poly std | DLA std |
|---|---|---|
| 4096 | ±0.40 | ±1.17 |
| 65536 | ±0.02 | ±1.31 |

**结论**：LS 矩阵 8192 足够（更大只是估计更稳）；**NN 方差与数据量无关 -> 是优化问题**。

### 7.4 压方差：零初始化 + 集成

| 方法 | K=1 | K=5 集成 |
|---|---|---|
| ILA | −56.19 ± 0.91 | −58.63 ± 0.87 |
| DLA | −56.94 ± 1.08 | **−59.83 ± 0.59** |

### 7.5 混合 DPD 与其量化

| | ACLR | EVM |
|---|---|---|
| float Hybrid | −70.46 | 0.0514% |
| **int16 Hybrid（含峰值余量）** | **−70.43** | 0.0515% |
| 损失 | **+0.04 dB** | +0.0001 pp |

隔离实验：numpy-float vs torch = **117 dB**（无 bug）；**NN 支路 int16 无损**（−0.02 dB）；损失全在多项式支路 -> 修好标定后归零。

### 7.6 TCN 量化

- 权重 int16：端到端无损（校正保真 80 dB）。
- 激活 per-tensor int16：损失 ~5.5 dB ACLR；**per-channel 无改善**（权重缩放自动抵消；各通道动态范围相近）。

## 8. 硬件与位真验证

| 模块 | 状态 |
|---|---|
| `nn_dpd_layer.v` 2 级流水 dense+ReLU+requant | PASS 位真 PASS |
| `nn_dpd_feat.v` 延迟线 + 整数 sqrt 特征前端 | PASS 位真 PASS |
| `nn_dpd_feat_par.v` P 样本/时钟并行前端（共享历史） | PASS P=2 PASS |
| `nn_dpd_par.v` P-lane 并行 MLP 顶层 | PASS 综合展开通过 |
| `nn_dpd_infer.c` MLP int16 推理 | PASS PASS |
| `hybrid_infer.c` Hybrid（MP 基 + MLP）推理 | PASS PASS（8 LSB）|
| `tcn_infer.c` TCN 卷积推理 | PASS PASS（0.7 LSB）|

## 9. 关键教训（TL;DR）

1. ILA 过驱动 -> 多项式外推爆炸；要留回退。
2. 复数 MAC 不能实虚分开累加。
3. `roll` 造 tap 后不能随机 batch。
4. 残差零初始化、多项式支路 LS 初始化。
5. 量化看**端到端 ΔACLR/ΔEVM**，不看内部 SQNR。
6. 定点 scale 必须**覆盖工作范围**（峰值削顶致命）。
7. 匹配 PA 上多项式是天花板；NN 的价值在**失配/漂移**场景。
8. 训练/标定用双精度；数据通路是否定点取决于目标平台。

## 10. 未完成 / 下一步

- 多相并行 RTL：`nn_dpd_par.v` 已展开通过；前端->MLP 的**特征->激活缩放**需折进首层权重（已定位）。
- TCN 激活需要更高位宽（权重单量化已无损）。
- 真实器件测量数据上的失配 PA 复现。
- 若目标平台允许浮点，可整体跳过量化工程。

---

*本报告由工程实测生成；所有数字可在仓库 `run*.log` 中复现。*
